[package]
name = "ams-api"
version = "0.1.0"
edition = "2024"

[dependencies]
actix-web = "4"
sea-orm = { version = "1.1.0", features = [
  "sqlx-sqlite",
  "runtime-tokio",
  "with-chrono",
  "with-uuid",
  "debug-print",
  "macros",
] }
